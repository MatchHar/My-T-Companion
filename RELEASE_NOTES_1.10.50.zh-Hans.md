# My T Companion 1.10.50

## 亮点
- 升级 Companion 时保留 `.env` 中的朋友同行设定（`install.sh` 不再丢掉 `FRIEND_TOGETHER_*`）。
- 若升级前已启用朋友同行，HostBox / My T 升级后应仍显示「已启用」，无需再手动启用。

## 升级
使用 HostBox，或在安装器管理的部署上运行 `./update.sh`。
