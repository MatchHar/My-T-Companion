# My T Companion 1.10.50

## 亮點
- 升級 Companion 時保留 `.env` 中的朋友同行設定（`install.sh` 不再丟掉 `FRIEND_TOGETHER_*`）。
- 若升級前已啟用朋友同行，HostBox / My T 升級後應仍顯示「已啟用」，無需再手動啟用。

## 升級
使用 HostBox，或在安裝器管理的部署上執行 `./update.sh`。
