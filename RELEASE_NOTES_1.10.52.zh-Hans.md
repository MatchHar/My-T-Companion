# Companion 1.10.52

- 统一认证接口 `/api/companion/v1/`。完成一次连接迁移后，普通新增功能不再需要逐项添加外部转发规则。
- 配合 HostBox 3.01 和新版 My T。原有接口暂时映射到相同处理逻辑；朋友同行访客和原 TeslaMate API 保持独立。
- 命令安装、更新与 HostBox 使用同一签名推荐目录及校验后的发布包。拒绝未知安装、普通降级、同版本校验冲突和并行更新。
- 保留车辆历史、凭证及通知配对。胎压历史使用 TeslaMate 实际记录的胎压与外温，不补造天气数据。

Cloudflare 远程管理隧道需要用 HostBox 3.01 修复连接一次，加入固定接口前缀。仅有隧道连接令牌的 Companion 无权修改 Cloudflare 帐户配置。现有访问保护保持不变，自定义规则冲突时停止，不强行覆盖。
