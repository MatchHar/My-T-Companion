# My T Companion 1.10.43

- 将完整停车事件文件的写入移出 MQTT 回调，让 TeslaMate 动作继续按顺序快速处理。
- 把短时间内的多次变化合并为一次持久化，并在服务正常停止时写入最新完整状态。
- 保留快速连续发生的四门开关动作及顺序，同时保留现有的各车门接收诊断。
- 车窗、上锁／解锁、插拔枪、充电、空调、预热、电池加热及充电口动作使用同一可靠流程。
- 不再为没有变化的普通遥测重复写盘；现有 API、配对、通知设置及停车历史无需迁移。

本版本改善的是 TeslaMate 发出观测后的保存过程。TeslaMate 从未发出的动作不会被补造，
也不会因此增加车辆轮询或唤醒车辆。
