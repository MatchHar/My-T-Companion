# My T Companion 1.10.27

## 每台 iPhone 独立可靠投递

- 中继暂时失败或 ActivityKit 会话令牌尚未登记时，事件会进入用户 VPS 上有上限
  的持久重试队列；一台 iPhone 投递成功，不会再掩盖另一台的失败。
- 无效中继安装只会暂停对应手机；暂停或解除配对会删除该手机的待重试记录。
- 锁车安全通知的设置与状态改为按安装 ID 独立保存。

## 数据库与并发加固

- PostgreSQL 连接超时修正为 10 秒，并加入 15 秒只读语句超时及请求级查询超时。
- 导航数据库补充查询移出 MQTT 状态锁，慢查询不会再阻塞车辆状态处理。
- 启动时重新验证已保存的中继网址、安装 ID 与密钥。

重试队列文件权限为 `0600`，总数上限 256，按事件类型在 10 分钟至 24 小时内
过期；投递成功、暂停、解除配对或到期时删除，并只保留在用户控制的 VPS 上。

本版也包含 1.10.26 发布后提交的安装程序可靠性修正。
