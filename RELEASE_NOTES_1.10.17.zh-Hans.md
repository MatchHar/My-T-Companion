# My T Companion 1.10.17

## 正式发布包同步修正

- 从已全面检查的最新源代码重新生成公开发布包，完整包含 GitHub／CDN
  临时下载失败重试及已验证的 Go 依赖更新。
- 使用 Go 1.26.5／Alpine 3.24，包含 `lib/pq` 1.12.3 与 `x/net` 0.56.0。
- Release 只发布精确的 1.10.17 压缩包和 SHA-256 校验文件；如果同名远程
  资产的摘要不同，发布会直接失败，不会覆盖或错误接受。
- 继续保留升级前备份和失败自动回滚流程。

没有 API 或存储数据迁移，继续兼容旧版 My T。

## 升级

```sh
sudo MY_T_VERSION=1.10.17 /opt/my-t-companion/update.sh
```
