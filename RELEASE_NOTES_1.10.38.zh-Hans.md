# My T Companion 1.10.38

## 四个车门的独立历史与最后观测状态

- 分别保存驾驶座前门、驾驶座后门、副驾驶座前门和副驾驶座后门未来发生的
  开启及关闭记录。
- 保留全部车门的汇总事件作为向后兼容的备用数据。兼容的 My T 版本会保留逐门
  记录，只隐藏同一批次中与之重复的汇总记录。
- `companion-status` 除现有四窗状态外，也返回最后观测到的车锁、全部车门及四门
  独立状态。

## 更可靠的停车数据与服务器路由

- 对仍在进行的在线停车区间，返回既有 30 分钟边界内最近的真实电量及额定续航
  观测，不再必须等待状态结束。
- 升级时修复 system Caddy、Docker Caddy、本机 API edge 及统一局域网配置中的
  `companion-status` 路由。Companion 仍只绑定回环地址，不会向公网开放 8083 端口。
- 软件更新事件加入已配对的数据来源标识，避免不同服务器使用相同 TeslaMate 本地
  车辆 ID 时发生事件冲突或在 My T 中打开错误连接。

无需迁移 TeslaMate 数据库、更换 Tesla 令牌、重新配对或唤醒车辆。现有历史不会
改变；升级前已经发生的逐门动作无法补回。旧版 My T 仍可使用汇总车门记录。
